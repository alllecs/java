public class Triangle {
	public static void main(String[] args) {
		int a = 5;
		int b = 8;
		int i = a * b / 2;
		double j = a + b + (Math.sqrt((a * a) + (b * b)));
		System.out.println("S = " + i + ", P = " + j);
	}
}
